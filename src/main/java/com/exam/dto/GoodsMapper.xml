<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="your.package.GoodsMapper">

    <select id="getGoodsById" resultType="Goods">
        SELECT * FROM Goods WHERE product_id = #{product_id}
    </select>

    <select id="getAllGoods" resultType="Goods">
        SELECT * FROM Goods
    </select>

    <insert id="insertGoods" useGeneratedKeys="true" keyProperty="product_id">
        INSERT INTO Goods(product_kind, product_name, product_stock, product_price, product_image)
        VALUES(#{product_kind}, #{product_name}, #{product_stock}, #{product_price}, #{product_image})
    </insert>

    <update id="updateGoods">
        UPDATE Goods SET
            product_kind = #{product_kind},
            product_name = #{product_name},
            product_stock = #{product_stock},
            product_price = #{product_price},
            product_image = #{product_image}
        WHERE product_id = #{product_id}
    </update>

    <delete id="deleteGoods">
        DELETE FROM Goods WHERE product_id = #{product_id}
    </delete>
    
</mapper>


