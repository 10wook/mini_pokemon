<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.exam.mapper.GoodsMapper">

<!-- Insert a new goods -->
    <insert id="insertGoods" parameterType="com.exam.model.Goods">
        INSERT INTO goods (product_id, product_kind, product_name, product_stock, product_image, product_price)
        VALUES (#{productId}, #{productKind}, #{productName}, #{productStock}, #{productImage}, #{productPrice})
    </insert>

    <!-- Update an existing goods -->
    <update id="updateGoods" parameterType="com.exam.model.Goods">
        UPDATE goods
        SET product_kind = #{productKind},
            product_name = #{productName},
            product_stock = #{productStock},
            product_image = #{productImage},
            product_price = #{productPrice}
        WHERE product_id = #{productId}
    </update>

    <!-- Delete goods by productId -->
    <delete id="deleteGoods" parameterType="int">
        DELETE FROM goods WHERE product_id = #{productId}
    </delete>

    <!-- Select goods by productId -->
    <select id="selectGoodsById" parameterType="int" resultType="com.exam.model.Goods">
        SELECT product_id AS productId,
               product_kind AS productKind,
               product_name AS productName,
               product_stock AS productStock,
               product_image AS productImage,
               product_price AS productPrice
        FROM goods
        WHERE product_id = #{productId}
    </select>

    <!-- Select all goods -->
    <select id="selectAllGoods" resultType="com.exam.model.Goods">
        SELECT product_id AS productId,
               product_kind AS productKind,
               product_name AS productName,
               product_stock AS productStock,
               product_image AS productImage,
               product_price AS productPrice
        FROM goods
    </select>

</mapper>