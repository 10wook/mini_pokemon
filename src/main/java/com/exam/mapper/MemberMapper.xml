<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.exam.mapper.MemberMapper">
 
 <!-- goodsmappper로 바꿀예정 -->
 
     <insert id="save" parameterType="product">
        insert into product (product_id, product_kind, product_name, product_stock, product_price, product_image)
        values (#{product_id}, #{product_kind}, #{product_name}, #{product_stock}, #{product_price}, #{product_image})
     </insert>
   
     <select id="findById" parameterType="Int" resultType="product">
        select product_id, product_kind, product_name, product_stock, product_price, product_image
        from product
        where product_id= #{product_id}
     </select>
     
     <delete id="remove" parameterType="Int">
     delete product_id, product_kind, product_name, product_stock, product_price, product_image
     from product
     where product_id= #{product_id}
     </delete>
</mapper>


